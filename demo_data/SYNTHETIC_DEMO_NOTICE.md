# Synthetic demonstration data

Every file below this directory is generated from fictional inputs created for
the public Orbital Perturbation Analyzer repository. The data does not describe
or reproduce a real spacecraft, operator, tracking station, orbit, or mission.

Reference trajectory CSV files can be reproduced with
`python scripts/generate_synthetic_reference_data.py`.
