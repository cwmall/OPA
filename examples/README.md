# Examples

Use this directory for small, reproducible OPA examples that rely only on redistributable data.

Each example should identify its required kernels or datasets, expected output, units, reference frame, epoch, and approximate runtime.
